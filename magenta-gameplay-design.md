# Bandwidth：Magenta 互动音乐机制草案

状态：设计提案，尚未实现；数值、时长和音乐效果均需要试玩验证。

## 1. 核心体验

两名玩家分别代表一种音乐风格，例如 Hip-hop 和 R&B。玩家一边争夺战场，一边争夺音乐的主导权：血量决定整首音乐的风格倾向，攻击与防守输入音符，道具和占领区改变音乐的走向。

设计目标是让玩家听得懂自己的行为如何影响音乐：谁占优势、谁发动大招、谁正在反击，都有可辨认的音乐反馈。

本草案先以双人对战说明规则。多人模式可以按队伍汇总风格权重，避免四种风格同时争夺导致反馈难以辨认。

## 2. 机制总览

| 机制 | 玩家行为 | 音乐反馈 | 主要控制方式 |
| --- | --- | --- | --- |
| 风格争夺 | 造成伤害、恢复血量 | 音乐逐渐偏向占优势的一方 | 风格嵌入混合权重 |
| 和弦激光 | 踩拍蓄力并释放激光 | 根音展开为完整和弦 | MIDI 音符起音、持续与释放 |
| 鼓点占领区 | 占领地图中央区域 | 鼓的活动更密集，形成高潮倾向 | 按时间调度鼓条件与鼓引导强度 |
| 旋律弹药 | 拾取音符并踩拍射击 | 多次攻击组成一句短旋律 | 按顺序发送 MIDI 音符 |
| 风格偷取 | 拾取采样器并命中对手 | 暂时把音乐拉向自己的风格 | 临时风格权重偏移 |
| 混沌核心 | 拾取短时强化道具 | 音乐出现更多随机变化 | 有界、平滑地调整 temperature |
| 和声护盾 | 格挡攻击并积累能量 | 和弦音被重新触发，满能量推动和声 | 和弦保持、音符重触发与和弦切换 |
| 合拍共鸣 | 双方在同一拍打出 Perfect | 两种风格短暂融合，共同触发和弦 | 风格权重暂时均衡、和弦事件 |

## 3. 具体机制

### 3.1 风格争夺：用血量争夺音乐主导权

- 玩家 A 代表 Hip-hop，玩家 B 代表 R&B。
- 基础权重由双方当前剩余血量计算：`A 权重 = A 血量 / (A 血量 + B 血量)`，B 取剩余权重。
- 例如双方满血时各占 50%；A 为 80 血、B 为 20 血时，目标混合权重为 80% Hip-hop、20% R&B。
- 权重变化经过平滑处理，让受伤后的音乐逐渐转向，避免每次命中都发生突变。
- 双方血量同时为零时不再计算比例，直接进入平局或回合结束逻辑。

视觉反馈：用一条双色风格条显示当前主导权，让玩家把听感与战况联系起来。

实现说明：混合的是风格条件，不是两条现成音轨的音量。80% 权重不保证听感恰好有 80% 的 Hip-hop 特征。

### 3.2 和弦激光：把一次大招变成音乐重音

- 在节拍附近开始蓄力，向音乐系统输入根音。
- 下一拍释放激光时，展开成完整和弦。
- 普通成功释放使用三和弦；Perfect 可以使用七和弦或九和弦，形成更丰富的和声。
- 蓄力取消或超时，需要释放此前持续的音符，避免条件卡住。

示例：在 C 小调中，蓄力时输入 C，释放时展开为 `C–E♭–G`；更高质量的释放可以展开为 `C–E♭–G–B♭`。

风格差异：Hip-hop 玩家偏低音与短促和弦，R&B 玩家偏丰满、持续较长的和弦。这是音乐设计目标，需结合风格提示和音符时值试听调校。

即时激光音效由游戏本地播放，和弦条件进入后续可生成的音乐时间段。生成缓冲意味着配乐变化可能稍晚听见，不能据此延迟伤害判定。

### 3.3 鼓点占领区：争夺音乐的高潮

- 地图中央设置可争夺的占领区。
- 无人占领时，使用较稀疏的鼓条件序列。
- 一方占领后，逐渐增加鼓条件的触发密度；持续占领可解锁更活跃的节奏型。
- 双方争夺时维持已有状态或切换到预设过渡型，避免条件频繁来回跳变。

这一机制鼓励玩家离开掩体参与争夺，也让整场音乐有更清楚的起伏。

实现说明：MRT2 的鼓条件不是指定某个鼓音色的采样器，也不能保证严格按指定 BPM 输出鼓点。用于战斗判定的节拍仍由统一时钟提供。

### 3.4 旋律弹药：用射击演奏短句

- 拾取一个包含三枚音符的弹药包，例如 `C → E♭ → G`。
- 接下来三次踩拍成功的射击依次消耗一个音符，并提交对应的 MIDI 起音。
- 初版建议以“射击判定达到 Good 或 Perfect”为触发条件，无需等待命中，保持玩家演奏动作与反馈的联系。
- 完成整句后触发一个收尾音或短暂的和声强调。
- 后续可增加不同旋律包，或者允许玩家选择音符顺序。

旋律弹药应使用当前调式内的音。它的核心奖励是完成乐句的听觉满足感，是否附带战斗增益可单独平衡。

### 3.5 风格偷取：让落后方拥有可听见的反击

- 拾取“采样器”后，下一次命中触发短时风格偏移。
- 例如 R&B 玩家血量落后，但成功命中后，音乐暂时更偏向 R&B。
- 效果到期后平滑回到按血量计算的基础权重。
- 权重偏移应设置上限，避免完全掩盖长期战况。

风格条同时显示基础占比和临时加成，帮助玩家理解为何音乐与血量暂时不一致。

### 3.6 混沌核心：短暂进入不可预测状态

- 拾取后获得一个短时间的高风险战斗强化，具体增益留待数值设计。
- 音乐侧适度提高采样温度 `temperature`，结束后平滑恢复。
- 通过特效和倒计时明确状态持续时间。

温度控制采样随机性，不等同于响度、速度或音乐强度。先在试听通过的小范围内变化，不用极端参数制造噪声，也不把它作为所有技能共用的“增强按钮”。

### 3.7 和声护盾：通过防守推动和声

- 拾取护盾后，向音乐系统保持一个和弦。
- 成功格挡时，重新触发其中一个音，同时给护盾充能。
- 满能量后切换到预设和弦进行的下一步。
- 护盾破裂或到期时释放所持音符。

这样防守也能成为一种演奏方式。它与激光共用音符控制，因此必须由音乐系统统一处理，避免护盾释放时误切断激光的音符。

### 3.8 合拍共鸣：对抗中的共同演奏

- 服务器确认双方在同一个指定拍位打出 Perfect 后，触发一次共鸣。
- 风格权重短暂趋近 50:50，同时输入一个预设和弦。
- 设置触发冷却，避免连续覆盖音乐。
- 初版可只提供视听奖励，保留竞技规则的可读性。

判定依据是服务器的节拍编号与攻击质量，不是两条网络消息是否同时到达。

## 4. 推荐第一版范围

优先实现四项：

1. **风格争夺**：验证整场战况能否被清楚地听出来。
2. **和弦激光**：验证一次明确操作能否产生可识别的音乐变化。
3. **旋律弹药**：验证玩家能否通过连续动作主动演奏。
4. **鼓点占领区**：验证地图争夺能否推动音乐的起伏。

风格偷取、混沌核心、和声护盾和合拍共鸣作为后续扩展。先把四种不同层次的反馈做清楚，再增加叠加效果。

## 5. 多个机制同时生效时的规则

- **风格**：先计算血量基础权重，再叠加采样器等临时偏移；共鸣作为短时目标覆盖，结束后恢复；最后归一化并平滑。
- **音符**：普通旋律与护盾条件由一个统一音符管理器合成；激光可暂时占据前景，限制同时活跃音符数量。每个来源独立记录起音、持续与释放。
- **鼓**：主要由占领状态控制，不让每发子弹都重写鼓序列。
- **随机性**：主要由混沌核心控制，避免与血量、激光、占领区同时争夺同一参数。
- **回合结束**：清理持续音符和临时效果，调度结束音乐；新回合重置音乐状态。

## 6. 与现有架构的衔接

遵循 [architecture.md](architecture.md) 中的权威边界和共享音乐时间线：

1. 主机确认命中、血量、Perfect、占领和道具等游戏事件。
2. 音乐决策层把这些事件转成风格权重、音符条件、鼓条件及临时效果。
3. Magenta 适配器在提前生成的时间线上应用这些条件。
4. 主机通过 `MusicPlan` 和配套音频传输，让客户端在相同时间播放同一份生成结果。

竞技判定始终使用 `BeatClock`。生成迟到时只调整未来音乐安排，不改变正在进行的节拍或战斗结果。即时武器音效与生成配乐分别调度，设备延迟只用于本地呈现补偿。

建议主机统一生成并分发音频；不能仅依靠相同提示词或 seed，假定多台设备独立推理一定产生相同声音。

当前 `MusicPlan` 提供通用 cue 容器，但上述机制、条件映射及音频服务仍需实现。

## 7. 试听与试玩验收

- 不看风格条时，玩家能否辨认音乐正在偏向哪一方。
- 激光和旋律弹药的音符变化是否足够明显，玩家能否把声音与动作对应起来。
- 鼓点占领是否带来可感知的变化，是否干扰固定节拍提示。
- 多个效果同时触发时，音乐是否仍可理解，而不是频繁突变。
- 联机双方是否听到同一份配乐，战斗判定是否独立于音频缓冲。
- 持续运行期间，生成速度能否跟上播放；若长期不足，应降低推理负担，单纯增大缓冲只能延后耗尽。

## 8. 参考资料

- [Magenta RealTime 2 官方技术介绍](https://magenta.withgoogle.com/magenta-realtime-2)：风格、音符和鼓条件，以及约 40 ms 一帧的生成控制。
- [MRT2 模型说明](https://github.com/magenta/magenta-realtime/blob/main/MODEL.md)：模型输入输出与 MIDI 条件表示。
- [Bandwidth 架构说明](architecture.md)：主机权威、BeatClock 与 MusicPlan。

上述机制是 Bandwidth 的设计提案。模型提供音乐生成与条件控制能力，具体玩法、触发规则及效果编排由游戏实现；40 ms 帧长不等于玩家动作到扬声器的端到端延迟。
